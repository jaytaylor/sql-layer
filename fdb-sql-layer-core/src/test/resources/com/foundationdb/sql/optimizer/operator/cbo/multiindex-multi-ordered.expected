PhysicalSelect[quan:int, quan:int]
  Project_Default(i1.quan, i2.quan)
    Map_NestedLoops(0)
      Flatten_HKeyOrdered(customers - orders INNER items)
        Flatten_HKeyOrdered(customers INNER orders)
          GroupLookup_Default(Index(items.sku) -> customers, orders, items)
            Intersect_Ordered(all, skip 1 left, skip 1 right, compare 1)
              IndexScan_Default(Index(items.sku), sku = '1234', orders.cid DESC, oid, iid)
              IndexScan_Default(Index(items.sku), sku = '4567', orders.cid DESC, oid, iid)
      Product_Nested(customers - orders - items x customers - orders - items)
        Filter_Default(customers - orders - items)
          Flatten_HKeyOrdered(customers - orders INNER items)
            Flatten_HKeyOrdered(customers INNER orders)
              Select_HKeyOrdered(i2.sku == '4567')
                BranchLookup_Nested(customers -> orders, items)