PhysicalSelect[INT(11), VARCHAR(32, UTF8, UCS_BINARY)][iid:int, oid:int, sku:varchar(32), quan:int]
  Project_Default(items.iid, items.oid, items.sku, items.quan)
    Filter_Default(items)
      Select_HKeyOrdered((items.quan == $1) AND (items.sku == $2))
        GroupScan_Default(customers)