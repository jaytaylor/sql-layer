duplicate-branches: Two occurrences of the same branch

join-child-twice: Two occurrences of the same child

multi-branch-conds-on-leaves-and-root: Conditions on each branch and on root

multi-branch-conds-on-leaves: Conditions only on two branches

single-branch-from-child: Single branch starting below root

single-branch-from-root: Single branch starting at root

two-idxs-one-table: Two single table indexes on same table

union-range: OR conditions indexed, The multiIndex will convert the union into a UnionOrdered
