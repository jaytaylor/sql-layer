accumulators: Not applied when there are accumulators.

implicit-select: Non group column in SELECT list.

implicit-sort: Non group column in ORDER BY (Project needed).

implicit-distinct: Written as DISTINCT (only briefly aggregate).

single: Single key, no aggregation.
