PhysicalSelect@1f9f0f2[name, order_date, name]
  Map_NestedLoops(loop_0)
    Flatten_HKeyOrdered(test.customers INNER test.orders)
      AncestorLookup_Default(test._akiban_customers -> test.customers, test.orders)
        IndexScan_Default(Index(test.orders.order_date[IndexColumn(order_date)]))
    Project_Default(Bound(0,Field(1)), Bound(0,Field(4)), Field(2))
      Select_HKeyOrdered(Field(2) != 'Smith')
        Filter_Default(test.child)
          BranchLookup_Default(test._akiban_parent -> test.parent (via test.parent))
            IndexScan_Default(Index(test.parent.name[IndexColumn(name)]))