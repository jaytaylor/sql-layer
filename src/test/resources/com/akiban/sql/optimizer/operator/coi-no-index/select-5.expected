PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE]]
  Project_Default(Field(1), Field(4))
    Sort_Tree(flatten(test.customers, test.orders))
      Filter_Default(flatten(test.customers, test.orders))
        Flatten_HKeyOrdered(test.customers INNER test.orders)
          GroupScan_Default(full scan on _akiban_customers)
