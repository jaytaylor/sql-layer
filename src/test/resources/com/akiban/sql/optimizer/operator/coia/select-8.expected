PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE], sku:varchar(32)[VARCHAR], quan:int[INT], state:char(2)[VARCHAR]]
  Project_Default(customers.name, orders.order_date, items.sku, items.quan, addresses.state)
    Product_NestedLoops(flatten(test.customers, test.addresses) x flatten(flatten(test.customers, test.orders), test.items))
      Filter_Default(flatten(test.customers, test.addresses))
        Flatten_HKeyOrdered(test.customers INNER test.addresses)
          GroupScan_Default(full scan on _akiban_customers)
      Filter_Default(flatten(flatten(test.customers, test.orders), test.items))
        Flatten_HKeyOrdered(flatten(test.customers, test.orders) INNER test.items)
          Flatten_HKeyOrdered(test.customers INNER test.orders)
            BranchLookup_Nested(_akiban_customers test.customers -> test.orders)