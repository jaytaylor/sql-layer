PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE], state:char(2)[VARCHAR]]
  Project_Default(customers.name, orders.order_date, addresses.state)
    Product_NestedLoops(test.customers - test.orders x test.customers - test.addresses)
      Flatten_HKeyOrdered(test.customers INNER test.orders)
        AncestorLookup_Default(test._akiban_customers -> test.customers, test.orders)
          IndexScan_Default(Index(test.orders.order_date[IndexColumn(order_date)]), order_date > 2011-01-01, cid, oid)
      Filter_Default(test.customers - test.addresses)
        Flatten_HKeyOrdered(test.customers INNER test.addresses)
          BranchLookup_Nested(test._akiban_customers -> test.addresses (via test.customers))