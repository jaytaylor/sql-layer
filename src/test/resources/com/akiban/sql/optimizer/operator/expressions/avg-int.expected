PhysicalSelect[_SQL_COL_1:DOUBLE]
  Project_Default(GROUP[0] / GROUP[1])
    Aggregate_Partial(sumINT, count(*))
      Project_Default(items.quan, 1)
        Filter_Default(test.items)
          GroupScan_Default(full scan on _akiban_customers)