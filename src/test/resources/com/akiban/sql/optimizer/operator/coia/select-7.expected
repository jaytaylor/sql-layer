PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE], sku:varchar(32)[VARCHAR], state:char(2)[VARCHAR]]
  Project_Default(Field(1), Field(4), Field(7), Field(11))
    Product_NestedLoops(flatten(flatten(test.customers, test.orders), test.items) x flatten(test.customers, test.addresses))
      Filter_Default(flatten(flatten(test.customers, test.orders), test.items))
        Flatten_HKeyOrdered(flatten(test.customers, test.orders) INNER test.items)
          Flatten_HKeyOrdered(test.customers INNER test.orders)
            AncestorLookup_Default(test.orders -> [test.customers])
              BranchLookup_Default(_akiban_customers Index(test.orders.order_date[IndexColumn(order_date)]) -> test.orders limit NO LIMIT)
                IndexScan_Default(Index(test.orders.order_date[IndexColumn(order_date)]) (>UnboundExpressions[Literal(2011-01-01)],<UnboundExpressions[Literal(NULL)]))
      Filter_Default(flatten(test.customers, test.addresses))
        Flatten_HKeyOrdered(test.customers INNER test.addresses)
          BranchLookup_Nested(_akiban_customers test.customers -> test.addresses)