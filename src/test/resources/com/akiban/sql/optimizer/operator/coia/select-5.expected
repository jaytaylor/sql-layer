PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE], state:char(2)[VARCHAR]]
  Project_Default(customers.name, orders.order_date, addresses.state)
    Product_NestedLoops(test.customers - test.addresses x test.customers - test.orders)
      Filter_Default(test.customers - test.addresses)
        Flatten_HKeyOrdered(test.customers INNER test.addresses)
          BranchLookup_Default(test._akiban_customers -> test.customers, test.addresses (via test.customers))
            IndexScan_Default(Index(test.customers.name[IndexColumn(name)]), name = IBM, cid)
      Filter_Default(test.customers - test.orders)
        Flatten_HKeyOrdered(test.customers INNER test.orders)
          BranchLookup_Nested(test._akiban_customers -> test.orders (via test.customers))