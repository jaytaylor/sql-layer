PhysicalSelect[oid:int[INT], cid:int[INT], order_date:date[DATE], iid:int[INT], oid:int[INT], sku:varchar(32)[VARCHAR], quan:int[INT]]
  Project_Default(orders.oid, orders.cid, orders.order_date, items.iid, items.oid, items.sku, items.quan)
    Flatten_HKeyOrdered(test.orders INNER test.items)
      AncestorLookup_Default(test._akiban_customers -> test.orders, test.items)
        Intersect_Ordered(skip 1 left, skip 1 right, compare 2)
          Union_Ordered()
            IndexScan_Default(Index(test.items.sku[IndexColumn(sku)]))
            IndexScan_Default(Index(test.items.sku[IndexColumn(sku)]))
          IndexScan_Default(Index(test.orders.order_date[IndexColumn(order_date)]), order_date = 2000-10-31, cid, oid)