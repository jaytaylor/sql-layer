project([Field(2)])
** Need to get both and outer join because orders is optional **
  AncestorLookup_Default(Index(.customers.cname_and_sku[IndexColumn(name), IndexColumn(sku)]) -> [user.orders])
    IndexScan_Default(Index(.customers.cname_and_sku[IndexColumn(name), IndexColumn(sku)]) (,))
[order_date:date[DATE]]