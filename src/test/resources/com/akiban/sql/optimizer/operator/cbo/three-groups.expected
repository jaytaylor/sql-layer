PhysicalSelect[cid:int[INT], name:varchar(32)[VARCHAR], oid:int[INT], cid:int[INT], order_date:date[DATE], id:int[INT], pid:int[INT], name:varchar(256)[VARCHAR], iid:int[INT], oid:int[INT], sku:varchar(32)[VARCHAR], quan:int[INT]]
  Map_NestedLoops(test.child)
    Filter_Default(test.child)
      GroupScan_Default(full scan on _akiban_parent)
    Map_NestedLoops(loop_1)
      Flatten_HKeyOrdered(test.customers INNER test.orders)
        AncestorLookup_Default(test._akiban_customers -> test.customers, test.orders)
          IndexScan_Default(Index(test.orders.PRIMARY[IndexColumn(oid)]), oid = child.pid, cid)
      Project_Default(customers.cid, customers.name, orders.oid, orders.cid, orders.order_date, child.id, child.pid, child.name, items.iid, items.oid, items.sku, items.quan)
        AncestorLookup_Default(test._akiban_customers -> test.items)
          IndexScan_Default(Index(test.items.__akiban_fk_1[IndexColumn(oid)]), oid = child.id, cid, iid)