PhysicalSelect[name:varchar(32)[VARCHAR], quan:int[INT]]
  project([Field(1), Field(8)])
    Sort_Tree(flatten(flatten(test.customers, test.orders), test.items))
      Filter_Default([flatten(flatten(test.customers, test.orders), test.items)])
        Flatten_HKeyOrdered(flatten(test.customers, test.orders) INNER test.items)
          Flatten_HKeyOrdered(test.customers INNER test.orders)
            AncestorLookup_Default(test.orders -> [test.customers])
              BranchLookup_Default(_akiban_customers Index(test.addresses.state[IndexColumn(state)]) -> test.orders limit NO LIMIT)
                IndexScan_Default(Index(test.addresses.state[IndexColumn(state)]) (>=UnboundExpressions[Literal(MA)],<=UnboundExpressions[Literal(MA)]))