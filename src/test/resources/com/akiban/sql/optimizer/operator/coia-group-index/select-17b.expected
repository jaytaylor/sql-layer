PhysicalSelect[name:varchar(32)[VARCHAR], quan:int[INT]]
  Sort_Tree(PROJECT[1] DESC, PRESERVE_DUPLICATES)
    Map_NestedLoops(loop_0)
      ValuesScan_Default(ImmutableRow[MA], ImmutableRow[NY], ImmutableRow[VT])
      Project_Default(customers.name, items.quan)
        Filter_Default(test.customers - test.orders - test.items)
          Flatten_HKeyOrdered(test.customers - test.orders INNER test.items)
            Flatten_HKeyOrdered(test.customers INNER test.orders)
              BranchLookup_Default(test._akiban_customers -> test.orders, test.items (via test.customers))
                AncestorLookup_Default(test._akiban_customers -> test.customers)
                  IndexScan_Default(Index(test.addresses.state[IndexColumn(state)]), state = VALUES[0], cid, aid)