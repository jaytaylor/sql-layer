PhysicalSelect[_SQL_COL_1:BIGINT NOT NULL]
  Project_Default(Field(0))
    Count_Default(*)
      Map_NestedLoops()
        Map_NestedLoops()
          Filter_Default(items)
            GroupScan_Default(customers)
          Project_Default(i1.iid + 1, i2.iid + 1)
            Filter_Default(items)
              Select_HKeyOrdered(i2.quan == i1.quan)
                GroupScan_Default(customers)
        Map_NestedLoops()
          IndexScan_Default(Index(customers.PRIMARY), cid = Bound(0, 0))
          IfEmpty_Default(NULL, NULL)
            IndexScan_Default(Index(orders.PRIMARY), oid = Bound(0, 1))