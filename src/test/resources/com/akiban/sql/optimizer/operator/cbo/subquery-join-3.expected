PhysicalSelect[_SQL_COL_1:INTEGER NOT NULL]
  Project_Default(GROUP[0])
    Count_Default(* FROM Index(test.orders.PRIMARY[IndexColumn(oid)]))
      Map_NestedLoops(loop_0)
        Map_NestedLoops(test.items)
          Filter_Default(test.items)
            GroupScan_Default(full scan on _akiban_customers)
          Project_Default(Bound(1,Field(0)) + 1, i2.iid + 1)
            Filter_Default(test.items)
              Select_HKeyOrdered(i2.quan == Bound(1,Field(3)))
                GroupScan_Default(full scan on _akiban_customers)
        Map_NestedLoops(loop_1)
          IndexScan_Default(Index(test.customers.PRIMARY[IndexColumn(cid)]), cid = v[0])
          IfEmpty_Default(NULL, NULL)
            IndexScan_Default(Index(test.orders.PRIMARY[IndexColumn(oid)]), oid = v[1], cid)