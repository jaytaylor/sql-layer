PhysicalSelect[_SQL_COL_1:INTEGER NOT NULL]
  Project_Default(GROUP[0])
    Count_Default(* FROM flatten(flatten(test.customers, test.orders), test.items))
      Flatten_HKeyOrdered(test.customers - test.orders INNER test.items)
        Flatten_HKeyOrdered(test.customers INNER test.orders)
          Select_HKeyOrdered(items.quan == 100)
            AncestorLookup_Default(test._akiban_customers -> test.customers, test.orders, test.items)
              IndexScan_Default(Index(test.items.sku[IndexColumn(sku)]), sku < 8888, cid, oid, iid)