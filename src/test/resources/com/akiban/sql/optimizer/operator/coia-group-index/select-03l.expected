PhysicalSelect[name:varchar(32)[VARCHAR], order_date:date[DATE], sku:varchar(32)[VARCHAR], quan:int[INT]]
  Project_Default(customers.name, orders.order_date, items.sku, items.quan)
    Flatten_HKeyOrdered(test.customers - test.orders LEFT test.items)
      Flatten_HKeyOrdered(test.customers INNER test.orders)
        AncestorLookup_Default(test._akiban_customers -> test.customers, test.orders, test.items)
          IndexScan_Default(Index(.customers.sku_and_date[IndexColumn(sku), IndexColumn(order_date)]), sku, order_date, cid, oid, iid)