PhysicalSelect[name:varchar(32)[VARCHAR]]
  project([Field(0)])
    Aggregation(GROUP BY 1 field, then: [])
      Sort_Tree(project([Field(1)]))
        project([Field(1)])
          Filter_Default([user.customers])
            GroupScan_Default(full scan on _akiban_customers)
