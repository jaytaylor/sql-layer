# Test to prove bug 857741 has been fixed
---
- Properties: sys-mysql
- suppressed: true
--- 
- CreateTable: customer (
     cid INT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
     name VARCHAR(255) NOT NULL,
     dob DATE NOT NULL)
---
- CreateTable: orders (
     oid INT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
     cid INT NOT NULL,
     order_date DATE NOT NULL,
     PRIMARY KEY (oid),
     GROUPING FOREIGN KEY (cid) REFERENCES customer)
---
- Statement: create index name_date on customer(customer.name, orders.order_date) USING LEFT JOIN
---
- Statement: create index idx_pad_2 on customer(name)
...